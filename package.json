{
  "name": "N-Back",
  "version": "1.0.0",
  "description": "just testing",
  "main": "index.js",
  "author": "Imran Khan",
  "license": "GPL-3.0-or-later",
  "private": true,
  "dependencies": {},
  "scripts": {
    "bundle": "esbuild --bundle index.js",
    "minify": "terser --compress --mangle"
  },
  "devDependencies": {
    "esbuild": "^0.6.5",
    "terser": "^4.8.1"
  }
}
